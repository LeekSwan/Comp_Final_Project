//creates a new list item

/*
export function renderSite(){
    const $root = $('#root');

    $(document).on('click', '#open', function(e){
        e.preventDefault();
        openNav();
    });
    $(document).on('click', '#fruits', function(e){
        e.preventDefault
        var fruithtml = renderFruit();
        var fruitList = document.createElement("div");
        fruithtml.innerHTML = fruitList;
    });
}


export function openNav() {
    document.getElementById("mySidenav").style.width = "250px";
}
  
export function renderList(){
    return `<ul id="fruitlist" style="background-color: blue; padding: 30px"> 
                <li>Apples</li>
                <li>Oranges</li>
                <li>Bananas</li>
            </ul>
        <input type="text" id="theInput"placeholder="type your important things here">
        <button type="button">+</button>`
}



export function createNewElement(){
    var list = document.createElement('li');
    var theInputValue = document.getElementById('theInput').value;
    var textNode = document.createTextNode(theInputValue);
    list.appendChild(textNode);

    if(theInputValue === " "){
        alert("Cannot be Empty!")
    } else {
        document.getElementById("list").append(list);
    }

    document.getElementById("theInput").value = "";

    const theX = document.createElement("SPAN");
    const text = document.createTextNode("close");
    theX.className = "close";
    theX.appendChild(txt);
    list.appendChild(theX);
}

$(function () {
    renderSite();
});

*/